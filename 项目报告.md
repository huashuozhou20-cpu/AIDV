# AIDV 数据库学习平台 — 项目报告

---

## 一、项目背景

随着大数据与人工智能技术的飞速发展，数据库技术作为信息系统的核心基础设施，其重要性日益凸显。SQL（结构化查询语言）是操作关系型数据库的标准语言，掌握 SQL 编程能力已成为计算机相关专业学生的基本素养。

然而，传统的 SQL 学习方式存在诸多痛点：学习平台与真实数据库环境割裂、练习题缺乏上下文（如表名、字段名）、初学者难以理解查询执行计划、缺乏个性化的练习题目生成机制。市面上虽有 SQL 学习网站，但大多依赖云端数据库，用户无法感知数据库底层运行机制。

在此背景下，我们设计并实现了 **AIDV（AI-powered Database Visualizer）**——一个集成了自研原生数据库引擎、AI 辅助教学、可视化数据管理于一体的数据库学习平台。该平台以中国人民大学数据库系统竞赛作品 RMDB 为底层引擎，结合大语言模型（LLM）实现智能出题与 SQL 解析，前端提供类 VS Code 的工作区管理体验，旨在为数据库教学提供一站式解决方案。

---

## 二、需求分析

### 2.1 功能性需求

| 需求编号 | 需求描述 | 优先级 |
|---------|---------|-------|
| F1 | 支持 SQL 语句的编写、执行与结果展示 | 高 |
| F2 | 支持数据库表结构的可视化浏览与管理 | 高 |
| F3 | 支持 AI 自动生成 SQL 练习题（含表名、字段名、示例数据） | 高 |
| F4 | 支持用户答案的自动验证（执行比对 + AI 判断） | 高 |
| F5 | 支持应用场景管理（AI 设计表结构、模板建表、手动设计） | 中 |
| F6 | 支持工作区文件夹层级管理（类 VS Code 资源管理器） | 中 |
| F7 | 支持文件夹级数据隔离（不同文件夹同名表不冲突） | 中 |
| F8 | 支持表结构在线修改（模拟 ALTER TABLE） | 中 |
| F9 | 支持中文字段名与中文表名 | 中 |
| F10 | 支持浏览器标签页式多查询编辑 | 中 |
| F11 | 支持 CSV 数据导入导出 | 低 |
| F12 | 支持查询执行计划可视化与 AI 解读 | 低 |

### 2.2 非功能性需求

- **性能**：单表 10 万条数据下实测查询响应时间：
  - 全表 COUNT：65.5ms
  - 主键等值查询：2.5ms
  - 范围查询（1% 数据）：50.0ms
  - 排序 + LIMIT 100：124.0ms
  - 聚合（AVG/MIN/MAX）：97.5ms
  - 批量插入吞吐：17,079 行/秒
- **可用性**：一键启动全栈服务（前端 + 后端 + 数据库引擎）
- **可扩展性**：模块化架构，AI 接口兼容 OpenAI 格式，可接入多种大模型
- **国际化**：支持中英文双语界面切换

---

## 三、项目目的

1. **教学目的**：为数据库课程提供一个集 SQL 练习、AI 辅助、可视化操作为一体的综合性教学平台
2. **展示目的**：展示自研 RMDB 数据库引擎的功能特性与架构设计
3. **实践目的**：通过全栈项目实践，掌握 React + FastAPI + C++ 三层架构的协同开发方法
4. **创新目的**：探索 AI 大模型在数据库教学领域的应用范式

---

## 四、项目总概

AIDV 是一个三层架构的 Web 应用：

```
┌─────────────────────────────────────────────┐
│               前端 (React 19 + TypeScript)    │
│         Vite + Tailwind CSS 4               │
│         24 文件 / ~4,988 行代码              │
├─────────────────────────────────────────────┤
│               后端 (Python FastAPI)          │
│         SQLite + HTTP/TCP 桥接              │
│         18 文件 / ~3,452 行代码              │
├─────────────────────────────────────────────┤
│          数据库引擎 (C++17 RMDB)             │
│    Flex/Bison SQL 解析 | B+树索引            │
│    WAL 日志 | 2PL 锁管理 | LRU 缓冲池       │
│         93 文件 / ~24,573 行代码             │
└─────────────────────────────────────────────┘
```

**总规模**：约 136 个源文件，33,000+ 行代码。

### 核心功能模块

| 模块 | 功能 |
|------|------|
| 数据工作台 | VS Code 风格资源管理器、浏览器标签页、SQL 编辑执行、结果分页 |
| 场景管理 | AI 智能建表、模板库、手动设计器、模拟 ALTER TABLE |
| SQL 练习 | AI 出题、自动验证、排行榜、拖拽分割面板 |
| AI 助手 | 自然语言转 SQL、执行计划解读、异常检测 |
| 查询分析器 | 执行计划可视化、AI 解读 |

---

## 五、设计要求

### 5.1 数据库引擎 (RMDB)

- 支持标准 SQL DDL/DML 语句（CREATE TABLE、SELECT、INSERT、DELETE、UPDATE、DROP TABLE 等）
- 支持多表 JOIN（INNER、LEFT、RIGHT、FULL、CROSS）
- 支持聚合查询（COUNT、SUM、AVG、MAX、MIN）+ GROUP BY / HAVING
- 支持 B+ 树索引（主键索引、唯一索引、普通索引）
- 支持事务管理（2PL 锁管理 + WAL 日志 + 死锁检测）
- 支持 AUTO_INCREMENT 自增主键
- 支持中文标识符（表名、列名）

### 5.2 后端服务

- RESTful API 设计，JWT 用户认证
- AI 接口兼容 OpenAI 格式，可接入 DeepSeek 等大模型
- SQLite 存储用户数据与工作区元数据
- HTTP REST API 为主 + TCP Socket 回退的双通道 RMDB 通信

### 5.3 前端界面

- 响应式设计，暗色主题
- 类 VS Code 的工作区树形管理
- 浏览器标签页式多查询编辑
- 可拖拽调整的面板布局
- 中英文双语支持

---

## 六、内容与方法

### 6.1 技术选型

| 层级 | 技术 | 选型理由 |
|------|------|---------|
| 数据库引擎 | C++17 | 高性能、底层控制，适合教学展示 |
| SQL 解析 | Flex 2.6 + Bison 3.8 | 经典词法/语法分析工具链 |
| 后端 | Python FastAPI | 异步支持、自动 API 文档、生态丰富 |
| AI 集成 | OpenAI SDK (DeepSeek) | 兼容 OpenAI 格式，成本低 |
| 前端 | React 19 + TypeScript | 类型安全、组件化、生态成熟 |
| 样式 | Tailwind CSS 4 | 原子化 CSS，快速构建 |
| 构建 | Vite 6 | 极速 HMR，ESBuild 打包 |

### 6.2 系统架构方法

- **RMDB 通信桥接**：后端通过 HTTP POST 向 RMDB 的 REST API 发送 SQL，RMDB 返回 ASCII 表格或 JSON 格式结果，后端解析后以 JSON 返回前端
- **AI 出题机制**：将数据库 DDL Schema 注入 AI Prompt，AI 根据难度生成含表名、字段名、示例数据表格的题目；答案验证采用双重机制——先执行比对结果集，不一致时回退 AI 判题
- **工作区隔离**：通过 SQLite 元数据表维护文件夹树与表映射；RMDB 表名自动加 `f{folderId}_` 前缀实现命名空间隔离；SQL 编辑器在文件夹上下文中自动改写 DML 表名
- **模拟 ALTER TABLE**：导出数据 → DROP TABLE → CREATE TABLE（新结构）→ 回插数据，含失败恢复机制

### 6.3 AI 应用方法

- **Prompt Engineering**：精心设计的系统提示，包含完整数据库 DDL 结构，要求 AI 输出包含表名、字段名和 Markdown 示例数据表格
- **两阶段验证**：确定性比对（执行两个 SQL 比较结果集）+ AI 语义判断（逻辑等价性检查）
- **难度分级**：简单（单表 WHERE）、中等（JOIN/GROUP BY）、困难（子查询/HAVING）

---

## 七、实际步骤

### 7.1 环境搭建

1. 安装 C++ 编译工具链（g++、CMake、Flex、Bison）
2. 安装 Python 依赖（FastAPI、uvicorn、openai 等）
3. 安装 Node.js 及前端依赖（React、Vite、Tailwind CSS）
4. 配置 AI API Key（DeepSeek）

### 7.2 数据库引擎构建

1. 修改 Flex 词法器 `lex.l`，扩展标识符规则支持 UTF-8 多字节字符
2. 修复 ASCII 表格输出的 CJK 字符宽度计算（`record_printer.h`）
3. 实现缓冲池页面清理机制，解决 DROP TABLE 后重建同名表的 "Duplicate key" 问题
4. 运行 `flex` 重新生成 `lex.yy.cpp`，CMake 编译 RMDB 二进制

### 7.3 后端开发

1. 设计并实现 RESTful API 路由结构（认证、查询、练习、场景、工作区、数据 CRUD）
2. 实现 RMDB HTTP REST API 通信层（含重试与 TCP 回退）
3. 实现 AI 出题与答案验证的 Prompt 工程
4. 实现工作区文件夹管理（SQLite 存储树结构 + RMDB 表名前缀隔离）
5. 实现模拟 ALTER TABLE（数据导出→删表→建表→回插）

### 7.4 前端开发

1. 搭建 React + TypeScript + Vite + Tailwind 项目骨架
2. 实现 VS Code 风格侧栏资源管理器（文件夹树、拖拽、行内重命名）
3. 实现浏览器标签页式 SQL 编辑器（多标签、可关闭、独立结果）
4. 实现可拖拽面板布局（侧栏、编辑器/结果分割线）
5. 实现 AI 出题界面与 Markdown 表格渲染
6. 实现中英文 i18n 国际化

### 7.5 集成与部署

1. 编写 `start.sh` 一键启动脚本（RMDB 守护进程 + 后端 + 前端构建）
2. 配置 `.gitignore` 排除构建产物，推送至 GitHub

---

## 八、RMDB 原生数据库引擎

### 8.1 概述

RMDB 是中国人民大学数据库系统竞赛的参赛作品，定位为一个教学型关系数据库管理系统内核。本项目在 RMDB 基础上进行了功能增强与 Bug 修复，使其更好地支撑上层 AIDV 平台的全部功能需求。

### 8.2 系统架构

```
┌──────────────────────────────────────────┐
│              SQL 语句输入                 │
├──────────────────────────────────────────┤
│  词法分析器 (Flex)  →  语法分析器 (Bison) │
├──────────────────────────────────────────┤
│          语义分析器 (Analyzer)            │
├──────────────────────────────────────────┤
│          查询优化器 (Optimizer)           │
├──────────────────────────────────────────┤
│          查询执行器 (Executor)            │
│   INSERT / DELETE / UPDATE / SELECT      │
├──────────────┬───────────────────────────┤
│  记录管理器   │      索引管理器           │
│  (RM)        │      (IX) B+树            │
├──────────────┴───────────────────────────┤
│           缓冲池 (Buffer Pool)            │
│           LRU 淘汰 / 256 MB              │
├──────────────────────────────────────────┤
│           磁盘管理器 (Disk Manager)       │
├──────────────────────────────────────────┤
│     锁管理器 (2PL)  │  日志管理器 (WAL)   │
└──────────────────────────────────────────┘
```

### 8.3 核心功能

**SQL 支持：**
- DDL: CREATE TABLE, DROP TABLE, CREATE INDEX, CREATE VIEW, DESC, SHOW TABLES
- DML: SELECT (DISTINCT, WHERE, GROUP BY, HAVING, ORDER BY, LIMIT), INSERT, DELETE, UPDATE
- JOIN: INNER, LEFT, RIGHT, FULL, CROSS
- 聚合: COUNT, SUM, AVG, MAX, MIN
- 表达式: 算术、比较、逻辑、IN、BETWEEN、LIKE、IS NULL、CONCAT
- 事务: BEGIN, COMMIT, ROLLBACK

**索引：**
- B+ 树索引结构，支持主键索引、唯一索引、普通索引
- 索引扫描执行器

**锁管理（严格两阶段锁）：**
- 多粒度锁（表级 + 行级）
- 5 种锁模式：S, X, IS, IX, SIX
- 基于 DFS 等待图的死锁检测（牺牲最年轻事务）
- 条件变量阻塞等待

**日志与恢复：**
- WAL（预写日志）机制
- 日志缓冲区 4 MB
- 后台每秒刷盘线程（组提交）

**缓冲池：**
- 256 MB 容量（65536 帧 × 4 KB）
- LRU 淘汰策略（双向链表 + 哈希表 O(1) 操作）

### 8.4 本项目的增强与修复

| 改进项 | 描述 |
|--------|------|
| 中文标识符支持 | 修改 Flex 词法器 `identifier` 规则，允许 `\x80-\xff` 字节范围，支持 UTF-8 中文表名/列名 |
| CJK 字符宽度修复 | 新增 `utf8_display_width`、`utf8_truncate`、`utf8_pad` 函数，修复 ASCII 表格输出中日韩字符对齐问题 |
| 缓冲池清理 | 在 `close_index` 和 `close_file` 中增加 `discard_pages` 调用，解决 DROP TABLE 后重建同名表的索引污染 |
| 约束顺序修复 | 确保生成的 DDL 中 `PRIMARY KEY` 在 `AUTO_INCREMENT` 之前，符合 RMDB 解析器语法 |

### 8.5 构建方式

```bash
cd rmdb/src/parser
flex --bison-bridge --bison-locations -o lex.yy.cpp lex.l
cd ../../build_new
cmake .. && cmake --build . -j
```

生成的 `rmdb` 二进制通过 `start.sh` 以守护进程方式启动，监听 TCP 8765 端口（回退）和 HTTP 8080 端口（主力 REST API）。

---

## 九、总结

AIDV 项目从底层 C++ 数据库引擎到上层 React 前端，完整实现了一个现代化数据库学习平台。项目展示了以下核心能力：

1. **底层系统编程**：C++17 实现数据库内核，包括 SQL 解析、B+ 树索引、WAL 日志、2PL 锁管理
2. **全栈 Web 开发**：FastAPI + React + TypeScript + Tailwind CSS 的现代技术栈
3. **AI 应用集成**：基于大语言模型的智能出题、SQL 解析、执行计划解读
4. **用户体验设计**：VS Code 风格工作区、浏览器标签页、拖拽面板、中英文双语

项目代码已开源，托管于 GitHub，可供数据库教学与学习使用。
